# docs

- [`architecture.svg`](architecture.svg) — the system diagram. Capture, the
  autonomous loop, the two doors, and every destination data reaches.
- [`architecture.md`](architecture.md) — the same thing in words, and the file
  where each claim is enforced.
